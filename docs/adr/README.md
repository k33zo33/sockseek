# Architecture Decision Records

Accepted decisions are immutable in intent. Change a decision by adding a new ADR that supersedes the previous one; do not silently edit architecture through implementation.
